main:
  push:
    docker:
      image: nginx:alpine
      volumes:
        - ./:/usr/share/nginx/html
  stages:
    - deploy
  deploy:
    stage: deploy
    script:
      - echo "检测到多页面结构更新 (Home, Hong_Kong, Travel, Memes)"
      - echo "静态网站部署成功"